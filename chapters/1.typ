#import "@preview/zero:0.5.0": num, zi

#v(50mm)

= Introduction #lorem(10)

This is report, we will explore the various factors that influence the performance of machine learning models. We will analyze datasets, evaluate algorithms, and provide insights into optimizing model accuracy and efficiency.

#pagebreak()

#text(size: 16pt, weight: "bold")[Abstract]

#lorem(300)

#pagebreak()

== Introduction

Physicists estimate a number of #num[1e80] particles in the observable universe. 
#let Js = zi.declare("J s")
Plancks constant is roughly #Js[6.626e-34] or #num("9.81+-0.1e2") or #num("1.0e-10"). 
#zi.Hz(1), #zi.kg(1), #zi.m(1), #zi.s(1), #zi.A(1), #zi.K(1), #zi.mol(1), #zi.cd(1)

#lorem(100)

+ The climate
  - Temperature
  - Percipitation
+ The topology
+ The geology

Glaciers as the one shown in @glaciers will cease to exists if we don't take action soon.

#figure(
  image("test.png", width: 10cm),
  caption: [
    _Glaciers_ form an important part of the earth's climate system.
  ]
) <glaciers>

Glaciers are vital indicators of climate change, and their retreat signals broader environmental shifts. Protecting these icy giants is crucial for maintaining global climate stability.

== Section 1

The equation $Q = rho A v + C $ defines the glacial flow rate, where $rho$ is the density of ice, $A$ is the cross-sectional area, $v$ is the velocity, and $C$ is a constant accounting for other factors.

The flow rate of glaciers is influenced by various factors including @eq1 temperature, snowfall, and underlying geology:

$ Q = rho A v + "time offset" $

The total displaced soil by glacial flow:

$ 7.32 beta + sum_(i=1)^nabla Q_i + Q_"i,c" - grave(a) / 2 $ <eq1>

=== Subsection 1.1

#lorem(600)